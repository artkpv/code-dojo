
Problem 12-3:
.. Show how to use a radix tree to sort S lexicographically in ~n time. For the example in Figure 12.5, the output of the sort should be the sequence 0, 011, 10, 100, 1011.


- Min in T tree?
 Tree-Minimum(T):
	 if T:
		 return T
	 if T.left:
		 return Tree-Minimum(T.left)
     return Tree-Minimum(T.right)

add_sorted(T, S):
  if T:
	  S.add(T)
  if T.left:
	  add_sorted(T.left, S)
  if T.right:
	  add_sorted(T.right, S)






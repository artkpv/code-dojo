TREE-DELETE(T, z)
  // For equal priority:
  if flag:  // 1/2 of calls - succ., 1/2 - predecessor
	  // using successor:
	  // ..
  else:
	  // using predecessor:
	  // ..
	  else  // two children:
		  y = TREE-MAX(z.left)
		  if z.left != y:
			  TRANSPLANT(T, y, y.left)
			  y.left = z.left
			  y.left.p = y
		  TRANSPLANT(T, z, y)
		  y.right = z.right
		  y.right.p = y
  flag = !flag


1. 1/2 of cases to predecessor
2. Randomly
3. Using height




